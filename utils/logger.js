// const config = require("config");
module.exports = {logger: require("debug")("app:startup")};

// For logging
// logger("Application Name: " + config.get("name"));
// logger("Mail Host: " + config.get("mail.host"));
// logger("Mail Password: " + process.env.lancer_password);